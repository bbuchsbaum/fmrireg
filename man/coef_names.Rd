% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/fmri_lm_methods.R
\name{coef_names}
\alias{coef_names}
\alias{coef_names.fmri_lm}
\title{Get Available Coefficient Names}
\usage{
coef_names(x, ...)

\method{coef_names}{fmri_lm}(x, type = c("estimates", "contrasts", "F", "all"), ...)
}
\arguments{
\item{x}{A fitted model object}

\item{...}{Additional arguments passed to methods}

\item{type}{Which set of names to return: \code{"estimates"} (default) for
event regressor names, \code{"contrasts"} for simple contrast names,
\code{"F"} for F-contrast names, or \code{"all"} for a named list of all
three.}
}
\value{
A character vector of coefficient names
}
\description{
Return the names of available coefficients from a fitted model object.
This helps users discover which coefficient names can be passed to
\code{\link{coef_image}} or other extraction functions.
}
\examples{
# Create a small example
X <- matrix(rnorm(50 * 4), 50, 4)
edata <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 12, 25, 38),
  run = c(1, 1, 1, 1)
)
dset <- fmridataset::matrix_dataset(X, TR = 2, run_length = 50,
                                    event_table = edata)
fit <- fmri_lm(onsets ~ hrf(condition), block = ~run, dataset = dset)
coef_names(fit)
}
\seealso{
\code{\link{coef_image}}, \code{\link{coef}}

Other statistical_measures: 
\code{\link{p_values}()},
\code{\link{standard_error}()},
\code{\link{stats}()}
}
\concept{statistical_measures}
