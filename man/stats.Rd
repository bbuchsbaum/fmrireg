% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/fmri_lm_methods.R, R/fmrilm.R
\name{stats}
\alias{stats}
\alias{stats.fmri_lm}
\title{Extract Test Statistics from a Model Fit}
\usage{
stats(x, ...)

\method{stats}{fmri_lm}(x, type = c("estimates", "contrasts", "F"), ...)

\method{stats}{fmri_lm}(x, type = c("estimates", "contrasts", "F"), ...)
}
\arguments{
\item{x}{The fitted model object}

\item{...}{Additional arguments passed to methods}

\item{type}{The type of statistics to extract: "estimates", "contrasts", or "F" (default: "estimates")}
}
\value{
A tibble or matrix containing test statistics
}
\description{
Extract test statistics (e.g., t-statistics, F-statistics) from a fitted model object.
This is part of a family of functions for extracting statistical measures.
}
\examples{
# Create example data
event_data <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 10, 20, 30),
  run = c(1, 1, 1, 1)
)

# Create sampling frame and dataset
sframe <- sampling_frame(blocklens = 50, TR = 2)
dset <- fmridataset::matrix_dataset(
  matrix(rnorm(100 * 2), 100, 2),
  TR = 2,
  run_length = 50,
  event_table = event_data
)

# Fit model
fit <- fmri_lm(
  onsets ~ hrf(condition),
  block = ~run,
  dataset = dset
)

# Extract test statistics
tstats <- stats(fit)
}
\seealso{
Other statistical_measures: 
\code{\link{p_values}()},
\code{\link{standard_error}()}
}
\concept{statistical_measures}
