% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_template.R
\name{baseline_spec}
\alias{baseline_spec}
\title{Baseline specification (subject-invariant)}
\usage{
baseline_spec(
  degree = 3,
  basis = c("bs", "poly", "ns", "constant"),
  confounds = NULL,
  intercept = c("runwise", "global", "none"),
  nuisance_check = c("warn", "error", "drop", "none")
)
}
\arguments{
\item{degree}{Integer drift degree passed to \code{baseline_model()}.}

\item{basis}{Drift basis: one of \code{"bs"}, \code{"poly"}, \code{"ns"},
\code{"constant"}.}

\item{confounds}{Optional confound selection used to populate the per-subject
nuisance regressors. Either \code{NULL} (no confounds), a character vector
of confound column names / patterns, or a \code{bidser} confound-set object.
Resolved to actual values by \code{from_bids()} / \code{instantiate()}.}

\item{intercept}{Intercept handling passed to \code{baseline_model()}.}

\item{nuisance_check}{How to handle problematic nuisance regressors, passed
to \code{baseline_model()}.}
}
\value{
An object of class \code{baseline_spec}.
}
\description{
Describes how the nuisance / baseline model should be built for every
subject, without binding any particular subject's confound values. The
concrete \code{baseline_model} is assembled per subject at instantiation
time from this spec, the subject's \code{sampling_frame}, and the subject's
confound matrix.
}
\examples{
baseline_spec(degree = 3, basis = "bs")
}
\seealso{
\code{\link[=fmri_template]{fmri_template()}}, \code{\link[=baseline_model]{baseline_model()}}
}
