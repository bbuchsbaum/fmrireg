% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_subspace_projection.R
\name{soft_subspace_options}
\alias{soft_subspace_options}
\title{Soft Subspace Control Options}
\usage{
soft_subspace_options(
  enabled = FALSE,
  nuisance_mask = NULL,
  nuisance_matrix = NULL,
  lambda = "auto",
  warn_redundant = TRUE
)
}
\arguments{
\item{enabled}{Logical. Whether to apply soft subspace projection.}

\item{nuisance_mask}{Path to NIfTI mask or logical vector indicating nuisance voxels.}

\item{nuisance_matrix}{Pre-computed nuisance timeseries matrix (alternative to mask).}

\item{lambda}{Ridge penalty: numeric, "auto", or "gcv".}

\item{warn_redundant}{Logical. Warn if baseline model contains nuisance terms.}
}
\value{
A list of class "soft_subspace_options".
}
\description{
Creates a configuration object for soft subspace projection.
}
\examples{
# Using a mask file
opts <- soft_subspace_options(
  enabled = TRUE,
  nuisance_mask = "path/to/wm_csf_mask.nii.gz",
  lambda = "auto"
)

# Using pre-computed nuisance matrix
N <- matrix(rnorm(100 * 20), 100, 20)
opts <- soft_subspace_options(
  enabled = TRUE,
  nuisance_matrix = N,
  lambda = 0.5
)
}
