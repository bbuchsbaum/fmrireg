% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R
\name{design_matrix.baseline_term}
\alias{design_matrix.baseline_term}
\title{Design matrix for a baseline term}
\usage{
\method{design_matrix}{baseline_term}(x, blockid = NULL, allrows = FALSE, ...)
}
\arguments{
\item{x}{A \code{baseline_term} object.}

\item{blockid}{Optional block identifier to subset rows.}

\item{allrows}{Logical; if \code{TRUE}, return all rows for the selected block.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A tibble containing baseline regressors.
}
\description{
Extract the design matrix from a single baseline term object.
}
\examples{
sframe <- sampling_frame(blocklens = 10, TR = 1)
bmod <- baseline_model(sframe = sframe)
dm <- design_matrix(baseline_terms(bmod)$drift)
}
