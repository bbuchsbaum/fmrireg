% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/fmri_latent_lm.R,
%   R/fmri_lm_methods.R, R/fmrilm.R
\name{standard_error}
\alias{standard_error}
\alias{standard_error.fmri_latent_lm}
\alias{standard_error.fmri_lm}
\title{Extract Standard Errors from a Model Fit}
\usage{
standard_error(x, ...)

\method{standard_error}{fmri_latent_lm}(x, type = c("estimates", "contrasts"), recon = FALSE, ...)

\method{standard_error}{fmri_lm}(x, type = c("estimates", "contrasts"), ...)

\method{standard_error}{fmri_lm}(x, type = c("estimates", "contrasts"), ...)
}
\arguments{
\item{x}{The fitted model object}

\item{...}{Additional arguments passed to methods}

\item{type}{The type of standard errors to extract: "estimates" or "contrasts" (default: "estimates")}

\item{recon}{Logical; whether to reconstruct the full matrix representation (default: FALSE)}
}
\value{
A tibble or matrix containing standard errors of parameter estimates
}
\description{
Extract standard errors of parameter estimates from a fitted model object.
This is part of a family of functions for extracting statistical measures.
}
\examples{
# Create example data
event_data <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 10, 20, 30),
  run = c(1, 1, 1, 1)
)

# Create sampling frame and dataset
sframe <- sampling_frame(blocklens = 50, TR = 2)
dset <- fmridataset::matrix_dataset(
  matrix(rnorm(100 * 2), 100, 2),
  TR = 2,
  run_length = 50,
  event_table = event_data
)

# Fit model
fit <- fmri_lm(
  onsets ~ hrf(condition),
  block = ~run,
  dataset = dset
)

# Extract standard errors
se <- standard_error(fit)
}
\seealso{
Other statistical_measures: 
\code{\link{p_values}()},
\code{\link{stats}()}
}
\concept{statistical_measures}
