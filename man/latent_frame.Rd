% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_frame_helpers.R
\name{latent_frame}
\alias{latent_frame}
\title{Build an fmri_frame from a latent (basis) decomposition}
\usage{
latent_frame(
  x,
  TR,
  run_length = NULL,
  event_table = NULL,
  censor = NULL,
  assay = "scores"
)
}
\arguments{
\item{x}{A \code{fmristore::LatentNeuroVec}, or a list with elements \code{basis}
(time by component scores), \code{loadings} (masked-voxel by component), and
\code{mask} (a \code{neuroim2::LogicalNeuroVol}).}

\item{TR}{Repetition time in seconds; one value, or one per run.}

\item{run_length}{Volumes per run; defaults to a single run spanning all
score rows.}

\item{event_table, censor}{As in \code{\link[=matrix_frame]{matrix_frame()}}.}

\item{assay}{Name of the assay.}
}
\value{
An \code{fmri_frame} over a synthesis-only \code{basis_space}.
}
\description{
A \code{fmristore::LatentNeuroVec} stores temporal scores (time by component)
and spatial loadings (voxel by component). This helper places the scores in
the assay and the loadings in a \code{\link[fmridataset:basis_space]{fmridataset::basis_space()}} whose parent
is the mask's \code{volume_space}, so fmrireg's latent engines can fit in
component space and reconstruct voxel maps through
\code{\link[fmridataset:basis-operators]{fmridataset::basis_synthesis()}}.
}
\seealso{
\code{\link[=matrix_frame]{matrix_frame()}}, \code{\link[fmridataset:basis_space_from_decoder]{fmridataset::basis_space_from_decoder()}}
}
