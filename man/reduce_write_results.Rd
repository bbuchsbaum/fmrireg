% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducers.R
\name{reduce_write_results}
\alias{reduce_write_results}
\title{Reducer: write per-subject results to disk (BIDS-keyed)}
\usage{
reduce_write_results(
  format = c("h5", "nifti", "gds"),
  stats = c("beta", "tstat"),
  contrasts = NULL,
  path = NULL,
  desc = "GLM",
  overwrite = FALSE
)
}
\arguments{
\item{format}{Output format passed to \code{\link[=write_results]{write_results()}}.}

\item{stats}{Which contrast statistics to write (mapped to
\code{contrast_stats}).}

\item{contrasts}{Optional restriction to specific contrasts.}

\item{path}{Output directory (passed to \code{\link[=write_results]{write_results()}}).}

\item{desc}{BIDS \code{desc-} label.}

\item{overwrite}{Overwrite existing files.}
}
\value{
A reducer function suitable for \code{\link[=fmri_template]{fmri_template()}}.
}
\description{
Calls \code{\link[=write_results]{write_results()}} on the fitted model, keying the output filenames by
the job's \code{meta} (\code{subject}, \code{task}, \code{space}). Returns the
vector of created file paths. This is the default reducer for large studies:
workers write compact statistical maps that \code{\link[=group_data]{group_data()}} / \code{\link[=fmri_meta]{fmri_meta()}}
can pick up for the group level.
}
\note{
\code{\link[=write_results]{write_results()}} requires BIDS entities: each job's \code{meta} should
carry \code{subject} (defaults to the job id), \code{task}, and
\code{space}. A missing \code{task}/\code{space} surfaces as an error when
the reducer runs.
}
\seealso{
Other reducers: 
\code{\link{reduce_betas}()},
\code{\link{reduce_contrasts}()},
\code{\link{reduce_identity}()}
}
\concept{reducers}
