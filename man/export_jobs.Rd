% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_jobs.R
\name{export_jobs}
\alias{export_jobs}
\title{Export jobs for external / array execution}
\usage{
export_jobs(
  jobs,
  dir,
  overwrite = FALSE,
  setup = "library(fmrireg)",
  results_dir = "results"
)
}
\arguments{
\item{jobs}{A single \link{fmri_job} or a list of them (e.g. from \code{\link[=instantiate]{instantiate()}}).}

\item{dir}{Output directory (created if needed).}

\item{overwrite}{Overwrite an existing manifest. Default \code{FALSE}.}

\item{setup}{Character vector of R lines run at the top of \code{run_one.R}
before jobs are executed (load packages, set threads, etc.). Default loads
\pkg{fmrireg}.}

\item{results_dir}{Default output subdirectory written by \code{run_one.R}.}
}
\value{
Invisibly, a list describing what was written (\code{dir},
\code{manifest}, \code{runner}, \code{n}, \code{ids}).
}
\description{
Writes a serialized job manifest plus a minimal, backend-agnostic
\code{run_one.R} runner to \code{dir}. Deliberately emits no scheduler code:
drive it with whatever array system you have, passing the 1-based job index
as the first argument, e.g. \code{Rscript run_one.R $SLURM_ARRAY_TASK_ID}.
}
\examples{
\dontrun{
jobs <- instantiate(tmpl, manifest)
export_jobs(jobs, "study/jobs")
# then, per array task:  Rscript study/jobs/run_one.R $SLURM_ARRAY_TASK_ID
}
}
\seealso{
\code{\link[=read_jobs]{read_jobs()}}, \code{\link[=run_jobs]{run_jobs()}}, \code{\link[=instantiate]{instantiate()}}
}
