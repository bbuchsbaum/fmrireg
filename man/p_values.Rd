% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/fmri_lm_methods.R
\name{p_values}
\alias{p_values}
\alias{p_values.fmri_lm}
\title{Extract P-values from a Model Fit}
\usage{
p_values(x, ...)

\method{p_values}{fmri_lm}(x, type = c("estimates", "contrasts"), ...)
}
\arguments{
\item{x}{The fitted model object}

\item{...}{Additional arguments passed to methods}

\item{type}{Character string specifying the type of p-values to extract.
Options typically include "estimates" for parameter estimates and "contrasts"
for contrast tests. Defaults to "estimates" in most methods.}
}
\value{
A tibble or matrix containing p-values
}
\description{
Extract p-values associated with parameter estimates or test statistics from a fitted model object.
This is part of a family of functions for extracting statistical measures.
}
\examples{
# Create example data
event_data <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 10, 20, 30),
  run = c(1, 1, 1, 1)
)

# Create sampling frame and dataset
sframe <- sampling_frame(blocklens = 50, TR = 2)
dset <- fmridataset::matrix_dataset(
  matrix(rnorm(50 * 2), 50, 2),
  TR = 2,
  run_length = 50,
  event_table = event_data
)

# Fit model
fit <- fmri_lm(
  onsets ~ hrf(condition),
  block = ~run,
  dataset = dset
)

# Extract p-values
pvals <- p_values(fit)
}
\seealso{
Other statistical_measures: 
\code{\link{standard_error}()},
\code{\link{stats}()}
}
\concept{statistical_measures}
