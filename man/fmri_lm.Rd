% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmrilm.R
\name{fmri_lm}
\alias{fmri_lm}
\alias{fmri_lm.formula}
\alias{fmri_lm.fmri_model}
\title{Fit a Linear Regression Model for fMRI Data Analysis}
\usage{
fmri_lm(formula, ...)

\method{fmri_lm}{formula}(
  formula,
  block,
  baseline_model = NULL,
  dataset,
  durations = 0,
  drop_empty = TRUE,
  control = fmri_lm_control(estimation = estimation_spec("runwise_meta")),
  compute = compute_spec(),
  engine = NULL,
  engine_args = list(),
  lowrank = NULL,
  ...
)

\method{fmri_lm}{fmri_model}(
  formula,
  dataset = NULL,
  control = fmri_lm_control(estimation = estimation_spec("runwise_meta")),
  compute = compute_spec(),
  engine = NULL,
  engine_args = list(),
  lowrank = NULL,
  ...
)
}
\arguments{
\item{formula}{A model formula describing the event structure or an
\code{fmri_model} object.}

\item{...}{Transitional legacy arguments retained for one compatibility
window. New code must express statistical choices in \code{control} and
execution choices in \code{compute}; unknown arguments are rejected.}

\item{block}{Formula describing run/block structure.}

\item{baseline_model}{Optional baseline/nuisance model.}

\item{dataset}{An \code{fmri_dataset}. For an \code{fmri_model} method this may be
omitted when the model already owns its dataset.}

\item{durations}{Event durations passed to model construction.}

\item{drop_empty}{Remove empty factor levels during model construction.}

\item{control}{A validated \code{\link[=fmri_lm_control]{fmri_lm_control()}}.}

\item{compute}{A validated \code{\link[=compute_spec]{compute_spec()}}.}

\item{engine}{Optional registered engine name.}

\item{engine_args}{Named list passed only to \code{engine}.}

\item{lowrank}{Optional \code{\link[=lowrank_control]{lowrank_control()}} for the latent-sketch engine.}
}
\value{
An object of class \code{fmri_lm}.
}
\description{
\code{fmri_lm} is a generic for fitting fMRI regression models. The
default interface accepts a model formula and dataset. An
alternative method can be used with a preconstructed
\code{fmri_model} object that already contains the design and data.

The public fitting boundary separates statistical choices in \code{\link[=fmri_lm_control]{fmri_lm_control()}}
from mechanical execution choices in \code{\link[=compute_spec]{compute_spec()}}. Optional engines use
the same control object and receive their private arguments through
\code{engine_args}.
}
