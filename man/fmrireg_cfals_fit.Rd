% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cfals_methods.R
\name{fmrireg_cfals_fit}
\alias{fmrireg_cfals_fit}
\title{Results from CFALS HRF estimation}
\usage{
  fmrireg_cfals_fit(h_coeffs, beta_amps, method, lambdas, call, hrf_basis,
    design_info, residuals, recon_hrf = NULL, gof = NULL)
}
\arguments{
  \item{h_coeffs}{Matrix of HRF basis coefficients (d x v).}
  \item{beta_amps}{Matrix of condition amplitudes (k x v).}
  \item{method}{Character string indicating the estimation method used.}
  \item{lambdas}{Numeric vector of regularisation parameters.}
  \item{call}{Matched call to \code{fmrireg_cfals}.}
  \item{hrf_basis}{HRF basis object used for the estimation.}
  \item{design_info}{List with design metadata (d, k, n, v, fullXtX).}
  \item{residuals}{Matrix of residuals from the projected data fit.}
  \item{recon_hrf}{Matrix of reconstructed HRF shapes.}
  \item{gof}{Numeric vector of goodness-of-fit statistics per voxel.}
}
\value{An object of class \code{fmrireg_cfals_fit}.}
\description{Container class used by \code{fmrireg_cfals()} to return estimated HRF
coefficients, condition amplitudes and related information.}
\examples{
  sframe <- sampling_frame(blocklens = 20, TR = 1)
  ev_df <- data.frame(onset = c(5, 15), block = 1)
  emod <- event_model(onset ~ hrf(constant), data = ev_df,
                      block = ~ block, sampling_frame = sframe)
  Y <- matrix(rnorm(20 * 1), 20, 1)
  fit <- fmrireg_cfals(Y, emod, HRF_SPMG1)
  str(fit)
}
