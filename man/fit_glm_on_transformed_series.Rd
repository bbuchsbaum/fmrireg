% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa_plugin_api.R
\name{fit_glm_on_transformed_series}
\alias{fit_glm_on_transformed_series}
\title{Fit the core GLM on a transformed time-series matrix}
\usage{
fit_glm_on_transformed_series(
  model,
  Y,
  cfg = NULL,
  dataset = NULL,
  strategy = "external",
  engine = "external"
)
}
\arguments{
\item{model}{An \code{fmri_model} describing the design.}

\item{Y}{Numeric matrix with \code{nrow(Y)} time points and columns matching voxels.}

\item{cfg}{Optional \code{fmri_lm_control}; defaults to \code{fmri_lm_control()} and
must request IID noise and no robust fitting.}

\item{dataset}{Optional dataset whose feature axis describes the columns of
\code{Y}. It is never inferred from \code{model$dataset}, because a transformed
response can have a different feature space.}

\item{strategy}{Character label recorded on the returned object. Defaults to "external".}

\item{engine}{Character label indicating the engine that produced the fit. Defaults to "external".}
}
\value{
An object of class \code{fmri_lm}.
}
\description{
This helper performs IID, non-robust OLS inference. It rejects controls that
request AR or robust fitting; use \code{\link[=fit_glm_with_config]{fit_glm_with_config()}} for those models.
}
