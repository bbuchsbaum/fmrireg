% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressor.R
\name{evaluate.single_trial_regressor}
\alias{evaluate.single_trial_regressor}
\title{Evaluate a single trial regressor}
\usage{
\method{evaluate}{single_trial_regressor}(x, grid, precision = 0.3, ...)
}
\arguments{
\item{x}{A single_trial_regressor object}

\item{grid}{Numeric vector specifying the time points at which to evaluate the regressor}

\item{precision}{Optional numeric value specifying the precision of the evaluation (default: 0.3)}

\item{...}{Additional arguments passed to the HRF evaluation function}
}
\value{
A numeric vector containing the predicted BOLD response at each time point in the grid
}
\description{
Method to evaluate a single trial regressor over a specified time grid.
This computes the predicted BOLD response for the trial at each time point
by convolving the trial event with its hemodynamic response function.
}
\examples{
# Create and evaluate a single trial regressor
reg <- single_trial_regressor(c(10), HRF_SPMG1)
grid <- seq(0, 30, by = 0.5)
response <- evaluate(reg, grid)

# Plot the response
plot(grid, response, type = "l", 
     xlab = "Time (s)", ylab = "BOLD Response")

}
\seealso{
\code{\link{single_trial_regressor}} for creating single trial regressors,
\code{\link{evaluate.HRF}} for HRF evaluation details
}
