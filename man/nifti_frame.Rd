% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_frame_helpers.R
\name{nifti_frame}
\alias{nifti_frame}
\title{Build a lazy fmri_frame from NIfTI files}
\usage{
nifti_frame(
  scans,
  mask,
  TR,
  run_length = NULL,
  event_table = NULL,
  censor = NULL,
  base_path = ".",
  assay = "bold"
)
}
\arguments{
\item{scans}{Character vector of NIfTI paths, one per run, all on the same
grid.}

\item{mask}{A NIfTI mask path, a \code{neuroim2::LogicalNeuroVol}, or a
\code{\link[fmridataset:volume_space]{fmridataset::volume_space()}}.}

\item{TR}{Repetition time in seconds; one value, or one per run.}

\item{run_length}{Volumes per run; defaults to the fourth header dimension
of each file.}

\item{event_table, censor}{As in \code{\link[=matrix_frame]{matrix_frame()}}.}

\item{base_path}{Directory that relative \code{scans} and \code{mask} paths are
resolved against.}

\item{assay}{Name of the assay.}
}
\value{
An \code{fmri_frame} over the NIfTI source's \code{volume_space}.
}
\description{
Wraps \code{\link[fmridataset:nifti_array_source]{fmridataset::nifti_array_source()}}, so headers and the mask are
read at construction but no BOLD volumes are read until the fit needs them.
For fMRIPrep derivatives see \code{\link[fmridataset:read_bids_bold]{fmridataset::read_bids_bold()}}.
}
\seealso{
\code{\link[=matrix_frame]{matrix_frame()}}, \code{\link[=neurovec_frame]{neurovec_frame()}},
\code{\link[fmridataset:nifti_array_source]{fmridataset::nifti_array_source()}}
}
