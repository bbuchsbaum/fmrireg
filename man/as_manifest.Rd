% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_bids.R
\name{as_manifest}
\alias{as_manifest}
\title{Coerce bindings to an fmri_manifest}
\usage{
as_manifest(x)
}
\arguments{
\item{x}{A list of bindings (named lists, each with at least \code{id},
\code{scans}, \code{TR}, \code{run_length}) or a manifest \code{data.frame}.}
}
\value{
An object of class \code{fmri_manifest}.
}
\description{
Wraps a list of bindings (or a manifest \code{data.frame}) into an
\code{fmri_manifest} for \code{\link[=instantiate]{instantiate()}}. This is the generic, BIDS-free way
to describe subjects; see \code{\link[=from_bids]{from_bids()}} for the BIDS populator.
}
\examples{
m <- as_manifest(list(
  list(id = "sub-01", scans = matrix(rnorm(80 * 2), 80, 2), TR = 2,
       run_length = c(40, 40),
       events = data.frame(onset = c(5, 45),
                           condition = factor(c("A", "B")), run = c(1, 2)))))
length(m)
}
\seealso{
\code{\link[=from_bids]{from_bids()}}, \code{\link[=instantiate]{instantiate()}}
}
