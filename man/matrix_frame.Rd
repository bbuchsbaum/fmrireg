% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_frame_helpers.R
\name{matrix_frame}
\alias{matrix_frame}
\title{Build an fmri_frame from an in-memory matrix}
\usage{
matrix_frame(
  datamat,
  TR,
  run_length,
  event_table = NULL,
  censor = NULL,
  feature_ids = NULL,
  assay = "signal"
)
}
\arguments{
\item{datamat}{A numeric matrix with one row per acquired volume and one
column per feature (voxel, vertex, parcel, or component).}

\item{TR}{Repetition time in seconds; one value, or one per run.}

\item{run_length}{Integer vector giving the number of volumes in each run.
Must sum to \code{nrow(datamat)}.}

\item{event_table}{Optional data frame of events (onsets, conditions, and
the block variable). It is stored on the frame as a keyed
\code{\link[fmridataset:event_table]{fmridataset::event_table()}}; an \code{event_id} column is added when absent.}

\item{censor}{Optional censoring indicator with one entry per volume: a
logical vector, a 0/1 vector, or a vector of 1-based volume indices.
Stored as the logical \code{censor} observation column that
\code{\link[fmridataset:temporal-schema]{fmridataset::temporal_schema()}} recognises.}

\item{feature_ids}{Optional stable feature IDs (one per column).}

\item{assay}{Name of the assay holding \code{datamat}.}
}
\value{
An \code{fmri_frame} whose feature space is an
\code{\link[fmridataset:index_space]{fmridataset::index_space()}}.
}
\description{
fmrireg fits models to \code{\link[fmridataset:fmri_frame]{fmridataset::fmri_frame()}} objects. This helper
builds one from a time-by-feature matrix plus the run structure and event
table that the design machinery needs. Observation IDs are derived
deterministically as \verb{run-<r>-vol-<index>}; feature IDs default to the
matrix column names when they are unique and to \verb{feature-<j>} otherwise.
}
\examples{
Y <- matrix(rnorm(80 * 3), 80, 3)
events <- data.frame(onset = c(5, 25, 45, 65),
                     condition = factor(c("A", "B", "A", "B")),
                     run = c(1, 1, 2, 2))
frame <- matrix_frame(Y, TR = 2, run_length = c(40, 40), event_table = events)
fmridataset::temporal_schema(frame)$run_lengths
}
\seealso{
\code{\link[=neurovec_frame]{neurovec_frame()}}, \code{\link[=nifti_frame]{nifti_frame()}}, \code{\link[=latent_frame]{latent_frame()}},
\code{\link[fmridataset:fmri_frame]{fmridataset::fmri_frame()}}
}
