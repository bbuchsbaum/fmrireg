% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_lm_methods.R, R/fmri_meta_methods.R
\name{coef_images.fmri_lm}
\alias{coef_images.fmri_lm}
\alias{coef_images}
\title{Extract Images/Volumes for Multiple Coefficients}
\usage{
\method{coef_images}{fmri_lm}(
  object,
  statistic = c("estimate", "se", "tstat", "prob"),
  type = c("estimates", "contrasts", "F"),
  coefs = NULL,
  ...
)

coef_images(object, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{statistic}{For \code{fmri_lm} objects: one of \code{"estimate"},
\code{"se"}, \code{"tstat"}, or \code{"prob"}.}

\item{type}{For \code{fmri_lm} objects: which coefficient set to extract:
\code{"estimates"} (default), \code{"contrasts"}, or \code{"F"}.}

\item{coefs}{Optional character vector of coefficient names (a subset of
\code{coef_names(object, type = type)}) to extract. Defaults to all
coefficients of the requested \code{type}.}

\item{...}{Additional arguments passed to methods (and on to
\code{\link{coef_image}}).}
}
\value{
A named list of \code{NeuroVol} objects (or numeric vectors when the
dataset is non-spatial), named by coefficient.
}
\description{
A plural companion to \code{\link{coef_image}}. Returns a named list of
volumes (or numeric vectors for non-spatial datasets), one per coefficient,
so callers can iterate over or write out every coefficient without
reimplementing the loop over \code{\link{coef_names}}.
}
\examples{
# Create a small example
X <- matrix(rnorm(50 * 4), 50, 4)
edata <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 12, 25, 38),
  run = c(1, 1, 1, 1)
)
dset <- fmridataset::matrix_dataset(X, TR = 2, run_length = 50,
                                    event_table = edata)
fit <- fmri_lm(onsets ~ hrf(condition), block = ~run, dataset = dset)
# Named list of one volume per event regressor
imgs <- coef_images(fit, statistic = "estimate", type = "estimates")
names(imgs)
}
\seealso{
\code{\link{coef_image}}, \code{\link{coef_names}}
}
