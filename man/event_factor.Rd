% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-classes.R
\name{event_factor}
\alias{event_factor}
\title{Create a categorical event sequence from a factor.}
\usage{
event_factor(fac, name, onsets, blockids = 1, durations = 0, subset = NULL)
}
\arguments{
\item{fac}{A factor or something coercible to a factor.}

\item{name}{Name of the event variable.}

\item{onsets}{Numeric vector of event onsets (seconds).}

\item{blockids}{Numeric vector of block IDs.}

\item{durations}{Numeric vector of event durations (seconds), or a scalar.}

\item{subset}{Optional logical vector indicating which events to keep. If
provided, the vector must match \code{onsets} in length and contain no \code{NA}
values.

Column names are sanitized using \code{.sanitizeName()} if provided. If
column names are missing or not unique, deterministic feature suffixes
(\code{f01}, \code{f02}, ...) are generated instead. The resulting names are
returned by \code{levels()} for the event object.}
}
\value{
An S3 object of class \code{event} and \code{event_seq}.
}
\description{
This is a user-facing wrapper around the internal \code{event()} constructor,
specifically for creating categorical event sequences from factors or characters.
}
\examples{
efac <- event_factor(factor(c("a", "b", "c", "a", "b", "c")), "abc", 
        onsets = seq(1, 100, length.out = 6))
print(efac)
levels(efac)

}
\seealso{
\code{\link{event_model}}, \code{\link{event_variable}}, \code{\link{event_matrix}}, \code{\link{event_basis}}
}
