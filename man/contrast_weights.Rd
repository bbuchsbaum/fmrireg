% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/event_model.R
\name{contrast_weights}
\alias{contrast_weights}
\alias{contrast_weights.convolved_term}
\alias{contrast_weights.fmri_model}
\alias{contrast_weights.event_model}
\title{Calculate contrast weights for a given contrast specification and term.}
\usage{
contrast_weights(x, ...)

\method{contrast_weights}{convolved_term}(x, ...)

\method{contrast_weights}{fmri_model}(x, ...)

\method{contrast_weights}{event_model}(x, ...)
}
\arguments{
\item{x}{The contrast specification object}

\item{...}{Extra arguments passed to specific methods}
}
\value{
A list containing:
\describe{
\item{term}{The model term the contrast is applied to}
\item{name}{The name of the contrast}
\item{weights}{A matrix of contrast weights}
\item{condnames}{The condition names associated with the weights}
\item{contrast_spec}{The original contrast specification}
}
}
\description{
This function calculates the contrast weights based on the contrast specification
provided by the user. It is a generic function that dispatches to the appropriate
method depending on the class of the contrast specification (e.g., unit_contrast_spec,
pair_contrast_spec, poly_contrast_spec, etc.).
}
\examples{
# Create a data frame with experimental design
event_data <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 10, 20, 80),
  run = c(1, 1, 1, 1)
)

# Create a sampling frame
sframe <- sampling_frame(blocklens = 50, TR = 2)

# Create an event model
evmodel <- event_model(
  onsets ~ hrf(condition),
  data = event_data,
  block = ~run,
  sampling_frame = sframe
)

# Create a contrast comparing conditions A and B
con <- pair_contrast(
  ~condition == "A",
  ~condition == "B",
  name = "A_vs_B"
)

# Calculate the contrast weights
weights <- contrast_weights(con, evmodel)
}
\seealso{
\code{\link[=pair_contrast]{pair_contrast()}}, \code{\link[=unit_contrast]{unit_contrast()}}, \code{\link[=poly_contrast]{poly_contrast()}}
}
\concept{contrast_weights}
