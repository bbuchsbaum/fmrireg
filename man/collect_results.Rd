% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_results.R
\name{collect_results}
\alias{collect_results}
\title{Collect per-subject result maps into a group_data object}
\usage{
collect_results(
  dir,
  space = NULL,
  beta_desc = "beta",
  se_desc = "se",
  format = c("nifti")
)
}
\arguments{
\item{dir}{Directory (searched recursively) containing the written maps.}

\item{space}{Optional BIDS space label to filter on (e.g.
\code{"MNI152NLin2009cAsym"}); \code{NULL} takes all.}

\item{beta_desc, se_desc}{The BIDS \code{desc-} labels of the effect and
standard-error maps (defaults \code{"beta"} / \code{"se"}).}

\item{format}{Group-data backend format (currently \code{"nifti"}).}
}
\value{
A \code{group_data} object.
}
\description{
Globs the per-subject statistical maps written by \code{\link[=reduce_write_results]{reduce_write_results()}}
under \code{dir}, pairs them by subject, and builds a \code{\link[=group_data]{group_data()}} object
for \code{\link[=fmri_meta]{fmri_meta()}}. When both beta and se maps are present the result carries
variance (suitable for inverse-variance meta-analysis); a beta-only result is
returned otherwise.
}
\note{
Pairing by BIDS subject requires \pkg{fmrigds} with BIDS-aware subject
keying. Multi-file \code{"h5"} aggregation is not yet supported upstream.
}
\examples{
\dontrun{
tmpl <- fmri_template(onset ~ hrf(trial_type, contrasts = con), ~ run,
                      reducer = reduce_write_results(format = "nifti",
                                                     stats = c("beta", "se"),
                                                     path = "study/glm"))
run_jobs(instantiate(tmpl, manifest))
gd <- collect_results("study/glm", space = "MNI152NLin2009cAsym")
fm <- fmri_meta(gd, ~ 1, method = "fe")
}
}
\seealso{
\code{\link[=reduce_write_results]{reduce_write_results()}}, \code{\link[=run_jobs]{run_jobs()}}, \code{\link[=fmri_meta]{fmri_meta()}}
}
