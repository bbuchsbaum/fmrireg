% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids_export.R
\name{write_results.fmri_lm}
\alias{write_results.fmri_lm}
\title{Write Results from fMRI Linear Model}
\usage{
\method{write_results}{fmri_lm}(
  x,
  path = NULL,
  subject = NULL,
  task = NULL,
  space = NULL,
  desc = "GLM",
  format = c("h5"),
  strategy = c("by_stat", "by_contrast"),
  save_betas = TRUE,
  contrasts = NULL,
  contrast_stats = c("beta", "tstat", "pval", "se"),
  overwrite = FALSE,
  validate_inputs = TRUE,
  ...
)
}
\arguments{
\item{x}{An fmri_lm object containing fitted model results}

\item{path}{Output directory path. If NULL, uses current working directory}

\item{subject}{Subject identifier (e.g., "01", "1001"). Required.}

\item{task}{Task identifier (e.g., "nback", "rest"). Required for BIDS compliance.}

\item{space}{Spatial reference (e.g., "MNI152NLin2009cAsym"). Optional but recommended.}

\item{desc}{Description of the analysis (default: "GLM")}

\item{format}{Character vector specifying output formats. Supported values
are \code{"h5"} (default BIDS/HDF5 export) and \code{"gds"} (fmrigds-compatible
assays plus an \code{.rds} plan describing the dataset). You may request both.}

\item{strategy}{Storage strategy: "by_stat" (group contrasts by statistic) or "by_contrast" (separate files)}

\item{save_betas}{Logical. Save raw regressor betas (default: TRUE)}

\item{contrasts}{Character vector of contrast names to save. NULL saves all contrasts}

\item{contrast_stats}{Character vector of contrast statistics to save (default: c("beta", "tstat", "pval", "se"))}

\item{overwrite}{Logical. Overwrite existing files (default: FALSE)}

\item{validate_inputs}{Logical. Validate fmrilm object structure (default: TRUE)}

\item{...}{Additional arguments passed to internal functions}
}
\value{
Invisible list of file paths created
}
\description{
Exports statistical maps from an fmri_lm object to HDF5 files with BIDS-compliant
naming and JSON metadata sidecars using the fmristore LabeledVolumeSet infrastructure.
}
\examples{
\dontrun{
# Save all results using default settings
write_results(fitted_model, subject = "01", task = "nback")

# Save only specific contrasts and statistics  
write_results(fitted_model, 
              subject = "01", task = "nback", space = "MNI152NLin2009cAsym",
              contrasts = c("FacesVsPlaces", "GoVsNoGo"),
              contrast_stats = c("beta", "tstat"))
}
}
