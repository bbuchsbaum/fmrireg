% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_lm_methods.R, R/fmri_meta_methods.R
\name{coef_image.fmri_lm}
\alias{coef_image.fmri_lm}
\alias{coef_image}
\title{Extract Image/Volume for Coefficient}
\usage{
\method{coef_image}{fmri_lm}(
  object,
  coef = 1,
  statistic = c("estimate", "se", "tstat", "prob"),
  type = c("estimates", "contrasts", "F"),
  ...
)

coef_image(object, coef = 1, statistic = c("estimate", "se", "z", "p"), ...)
}
\arguments{
\item{object}{An fmri_meta object}

\item{coef}{Coefficient name or index}

\item{statistic}{Type of statistic to extract ("estimate", "se", "z", "p")}

\item{type}{For \code{fmri_lm} objects: which coefficient set to index into:
\code{"estimates"} (default), \code{"contrasts"}, or \code{"F"}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
NeuroVol object or matrix
}
\description{
Extract Image/Volume for Coefficient
}
\examples{
# Create a small example
X <- matrix(rnorm(50 * 4), 50, 4)
edata <- data.frame(
  condition = factor(c("A", "B", "A", "B")),
  onsets = c(1, 12, 25, 38),
  run = c(1, 1, 1, 1)
)
dset <- fmridataset::matrix_dataset(X, TR = 2, run_length = 50,
                                    event_table = edata)
fit <- fmri_lm(onsets ~ hrf(condition), block = ~run, dataset = dset)
# Get coefficient estimates as a numeric vector
coef_image(fit, coef = 1)
toy_meta <- structure(
  list(
    coefficients = matrix(c(0.3, 0.1), nrow = 1,
      dimnames = list(NULL, c("A", "B"))),
    se = matrix(c(0.05, 0.06), nrow = 1)
  ),
  class = "fmri_meta"
)
coef_image(toy_meta, coef = "A")
}
