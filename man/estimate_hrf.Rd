% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_hrf.R
\name{estimate_hrf}
\alias{estimate_hrf}
\title{Estimate smooth condition-level hemodynamic response functions}
\usage{
estimate_hrf(
  form,
  fixed = NULL,
  block,
  dataset,
  bs = NULL,
  rsam = seq(0, 20, by = 1),
  basemod = NULL,
  k = 8L,
  fx = NULL,
  progress = FALSE,
  basis = c("bspline", "tent"),
  lambda = "gcv",
  lambda_grid = c(0, 10^seq(-4, 4, length.out = 25L)),
  ci_level = 0.95
)
}
\arguments{
\item{form}{A two-sided event-model formula containing one or more \code{hrf()}
terms. \code{trialwise()} terms are rejected because this function estimates
condition-level curves.}

\item{fixed}{Optional event-model formula whose design is treated as nuisance.}

\item{block}{Formula identifying acquisition runs or blocks.}

\item{dataset}{An \code{fmri_dataset}.}

\item{bs}{Deprecated legacy basis selector. Values from the former GAM API are
mapped to \code{basis = "bspline"}.}

\item{rsam}{Strictly increasing, finite post-stimulus times beginning at zero.}

\item{basemod}{Optional baseline model. The default is a constant baseline.}

\item{k}{Number of free HRF basis functions per curve. At least four for the
default cubic B-spline basis.}

\item{fx}{Deprecated legacy selector. \code{fx = TRUE} maps to \code{lambda = 0} when
\code{lambda} is omitted.}

\item{progress}{Show progress while scanning the GCV grid.}

\item{basis}{Either \code{"bspline"} (cubic) or \code{"tent"} (piecewise linear).}

\item{lambda}{Non-negative smoothing strength or \code{"gcv"} for shared automatic
selection.}

\item{lambda_grid}{Candidate smoothing strengths used for GCV.}

\item{ci_level}{Confidence level in \verb{(0, 1)}, or \code{NULL} to omit intervals.}
}
\value{
An object of class \code{fmri_hrf_estimate}. Its \code{estimate} and
\code{std.error} arrays have dimensions time by curve by voxel. The object also
contains labeled curve metadata, basis coefficients, smoothing diagnostics,
and the designs used for estimation. Use \code{tidy()}, \code{predict()}, \code{coef()}, or
\code{as.matrix()} for common downstream representations.
}
\description{
\code{estimate_hrf()} estimates condition-level HRF curves directly from an fMRI
dataset. It builds an event-aligned spline basis, removes baseline and fixed
nuisance effects once, and solves every voxel together with one penalized
multiresponse linear system. This replaces the former voxel-by-voxel GAM
implementation, whose predictor did not represent post-stimulus time.
}
\details{
A single smoothing parameter is shared across voxels. With \code{lambda = "gcv"},
it is selected by scale-normalized generalized cross-validation so high-
variance voxels do not dominate the choice. Curves are constrained to zero at
the beginning and end of \code{rsam}. Standard errors and confidence intervals use
the fitted penalized-linear-model covariance under independent, homoscedastic
time-point errors; they are not autocorrelation-robust.
}
\examples{
set.seed(18)
n <- 80L
events <- data.frame(
  onset = seq(6, 62, by = 8),
  condition = factor(rep(c("A", "B"), 4)),
  run = 1L
)
dataset <- fmridataset::matrix_dataset(
  matrix(rnorm(n * 2), nrow = n),
  TR = 1,
  run_length = n,
  event_table = events
)
fit <- estimate_hrf(
  onset ~ hrf(condition),
  block = ~run,
  dataset = dataset,
  rsam = 0:12,
  k = 6,
  lambda = 1
)
tidy(fit, voxel = 1)
}
\seealso{
\code{\link[=fitted_hrf]{fitted_hrf()}}, \code{\link[=event_model]{event_model()}}, \code{\link[=baseline_model]{baseline_model()}}
}
