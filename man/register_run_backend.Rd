% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_backends.R
\name{register_run_backend}
\alias{register_run_backend}
\title{Register an execution backend for run_jobs()}
\usage{
register_run_backend(name, fn, overwrite = FALSE)
}
\arguments{
\item{name}{Backend name (a string).}

\item{fn}{A function \code{function(jobs, run_one, ...)} returning an
order-preserving list of per-job results.}

\item{overwrite}{Allow replacing an existing backend of the same name.}
}
\value{
The backend name, invisibly.
}
\description{
Adds a named backend that \code{\link[=run_jobs]{run_jobs()}} can dispatch to. This is how to plug in
a custom scheduler driver without modifying \pkg{fmrireg}. The builtins
\code{"sequential"} and \code{"future"} are always available.
}
\examples{
register_run_backend("first_only", function(jobs, run_one, ...) {
  list(run_one(jobs[[1]]))
})
}
\seealso{
\code{\link[=run_jobs]{run_jobs()}}, \code{\link[=run_backends]{run_backends()}}
}
