% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-classes.R
\name{elements.event}
\alias{elements.event}
\title{Extract elements (values or labels) of an event object}
\usage{
\method{elements}{event}(x, what = c("values", "labels"), transformed = TRUE, ...)
}
\arguments{
\item{x}{An event object.}

\item{what}{Character, either "values" (default) to return the numeric data matrix
(from \verb{$value}), or "labels" to return descriptive names/levels for each
event instance (e.g., factor levels as a factor vector, or column names/
basis levels as a character vector or matrix).}

\item{transformed}{Logical; relevant only for basis events when \code{what="values"}.
If TRUE (default), return the transformed basis matrix (\verb{$value}).
If FALSE, attempt to return the original pre-transformation values (not robustly supported).}

\item{...}{Additional arguments (unused).}
}
\value{
A named list containing one element (a matrix or vector).
If \code{what="values"}, an N x K numeric matrix.
If \code{what="labels"}, an N-length factor/vector or N x K character matrix.
The list element name is \code{x$varname}.
}
\description{
Returns a list containing the event's data, either the actual numeric values
or descriptive labels. Used internally by \code{event_term} and potentially for inspection.
}
