% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_frame_helpers.R
\name{neurovec_frame}
\alias{neurovec_frame}
\title{Build an fmri_frame from in-memory NeuroVec objects}
\usage{
neurovec_frame(
  scans,
  mask,
  TR,
  run_length = NULL,
  event_table = NULL,
  censor = NULL,
  assay = "bold"
)
}
\arguments{
\item{scans}{A \code{NeuroVec}, or a list of them, one per run.}

\item{mask}{A \code{neuroim2::LogicalNeuroVol} (or a \code{NeuroVol} whose non-zero
voxels form the mask) on the same 3D grid as \code{scans}.}

\item{TR}{Repetition time in seconds; one value, or one per run.}

\item{run_length}{Volumes per run; defaults to the fourth dimension of each
scan.}

\item{event_table, censor}{As in \code{\link[=matrix_frame]{matrix_frame()}}.}

\item{assay}{Name of the assay.}
}
\value{
An \code{fmri_frame} over a \code{volume_space}.
}
\description{
Extracts the masked time series of one or more \code{neuroim2::NeuroVec} runs
and builds an \code{\link[fmridataset:fmri_frame]{fmridataset::fmri_frame()}} whose feature space is the
\code{\link[fmridataset:volume_space]{fmridataset::volume_space()}} described by \code{mask}. Voxel time series are
read with \code{neuroim2::series()}, so a \code{SparseNeuroVec} does not need to be
densified.
}
\seealso{
\code{\link[=matrix_frame]{matrix_frame()}}, \code{\link[=nifti_frame]{nifti_frame()}}
}
