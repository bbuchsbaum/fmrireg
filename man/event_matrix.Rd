% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_vector.R
\name{event_matrix}
\alias{event_matrix}
\title{Create a continuous event set from a matrix.}
\usage{
event_matrix(mat, name, onsets, blockids = rep(1, ncol(mat)), durations = NULL)
}
\arguments{
\item{mat}{A matrix of continuous event values (one row per event).}

\item{name}{Name for the event set.}

\item{onsets}{Numeric vector of onsets.}

\item{blockids}{Numeric vector of block IDs (default: rep(1, ncol(mat))).}

\item{durations}{Numeric vector of durations (default: NULL).}
}
\value{
An object of class "event_matrix" and "event_seq".
}
\description{
Create a continuous event set from a matrix.
}
\examples{
mat <- matrix(rnorm(200), 100, 2)
onsets <- seq(1, 1000, length.out = 100)
durations <- rep(1, 100)
blockids <- rep(1, 100)
eset <- event_matrix(mat, "eset", onsets, durations, blockids)

}
