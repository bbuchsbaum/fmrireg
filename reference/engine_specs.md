# List Registered Engine Specifications

Returns the read-only specifications for all currently registered
engines.

## Usage

``` r
engine_specs()
```

## Value

A named list of `fmrireg_engine_spec` objects.
