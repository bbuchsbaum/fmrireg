# Detect Input Format for Group Data

Detect Input Format for Group Data

## Usage

``` r
detect_group_data_format(data)
```

## Arguments

- data:

  Input data to detect format from

## Value

Character string indicating detected format
