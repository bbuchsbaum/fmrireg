# Extract the structured variance model

Extract the structured variance model

## Usage

``` r
variance_model(x)
```

## Arguments

- x:

  A fitted `fmri_lm` object.

## Value

An `fmri_lm_variance_model` object.
