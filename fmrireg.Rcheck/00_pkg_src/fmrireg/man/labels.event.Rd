% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-classes.R
\name{labels.event}
\alias{labels.event}
\title{Get Formatted Labels for a Single Event}
\usage{
\method{labels}{event}(x, ...)
}
\arguments{
\item{x}{An object of class \code{event}.}

\item{...}{Additional arguments (unused).}
}
\value{
A character vector of formatted labels, or \code{character(0)} if not applicable.
}
\description{
Returns a character vector of formatted labels for an event object,
using the \code{Variable[Level]} style for categorical events,
\code{Variable[Index]} for multi-column continuous events, or just
\code{Variable} for single continuous events.
Useful for getting consistent labels for individual event components.
This is distinct from \code{levels()} which returns the raw level names or column names.
Relies on the internal \code{.level_vector} helper function.
}
\examples{
fac <- factor(rep(c("A", "B"), 3))
onsets <- 1:6
ev_fac <- event_factor(fac, "Condition", onsets)
labels(ev_fac) # Should return c("Condition[A]", "Condition[B]")

vals <- 1:6
ev_num <- event_variable(vals, "Modulator", onsets)
labels(ev_num) # Should return "Modulator"

mat <- matrix(1:12, 6, 2)
colnames(mat) <- c("C1", "C2")
ev_mat <- event_matrix(mat, "MatrixVar", onsets)
labels(ev_mat) # Should return c("MatrixVar[1]", "MatrixVar[2]") 

}
