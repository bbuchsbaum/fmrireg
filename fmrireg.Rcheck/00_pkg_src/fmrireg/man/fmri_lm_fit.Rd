% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmrilm.R, R/fmrilm_new.R
\name{fmri_lm_fit}
\alias{fmri_lm_fit}
\title{Fit an fMRI Linear Regression Model with a Specified Fitting Strategy}
\usage{
fmri_lm_fit(
  fmrimod,
  dataset,
  strategy = c("runwise", "chunkwise"),
  nchunks = 10,
  cfg,
  extra_nuisance = NULL,
  keep_extra_nuisance_in_model = FALSE,
  use_fast_path = FALSE,
  verbose = FALSE,
  progress = FALSE
)

fmri_lm_fit(
  fmrimod,
  dataset,
  strategy = c("runwise", "chunkwise"),
  nchunks = 10,
  cfg,
  extra_nuisance = NULL,
  keep_extra_nuisance_in_model = FALSE,
  use_fast_path = FALSE,
  verbose = FALSE,
  progress = FALSE
)
}
\arguments{
\item{fmrimod}{An fmri_model object}

\item{dataset}{An fmri_dataset object}

\item{strategy}{Fitting strategy: "runwise" or "chunkwise"}

\item{nchunks}{Number of chunks for chunkwise strategy}

\item{cfg}{An fmri_lm_config object with all fitting options}

\item{extra_nuisance}{Additional nuisance regressors}

\item{keep_extra_nuisance_in_model}{Whether to keep extra nuisance}

\item{use_fast_path}{Use optimized computations}

\item{verbose}{Print progress messages}

\item{progress}{Show progress bar}

\item{parallel_voxels}{Logical. If TRUE, voxelwise AR processing within runs
is parallelised using \code{future.apply}. Default is \code{FALSE}.}

\item{...}{Additional arguments.}
}
\value{
A fitted fMRI linear regression model with the specified fitting strategy.

List with fitting results
}
\description{
This function fits an fMRI linear regression model using the specified \code{fmri_model} object, dataset,
and data splitting strategy (either \code{"runwise"} or \code{"chunkwise"}). It is primarily an internal function
used by the \code{fmri_lm} function.

Internal function that performs the actual model fitting.
}
\seealso{
\code{\link{fmri_lm}}, \code{\link{fmri_model}}, \code{\link{fmri_dataset}}
}
\keyword{internal}
