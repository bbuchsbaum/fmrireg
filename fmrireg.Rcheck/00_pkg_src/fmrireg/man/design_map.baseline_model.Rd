% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R
\name{design_map.baseline_model}
\alias{design_map.baseline_model}
\title{Heatmap visualization of the baseline_model design matrix}
\usage{
\method{design_map}{baseline_model}(
  x,
  block_separators = TRUE,
  rotate_x_text = TRUE,
  fill_midpoint = NULL,
  fill_limits = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{baseline_model} object.}

\item{block_separators}{Logical; if \code{TRUE}, draw white horizontal lines between blocks.}

\item{rotate_x_text}{Logical; if \code{TRUE}, rotate x-axis labels by 45 degrees.}

\item{fill_midpoint}{Numeric or \code{NULL}; if not \code{NULL}, used as the \code{midpoint} in
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient2()}} to center the color scale (for example at 0).}

\item{fill_limits}{Numeric vector of length 2 or \code{NULL}; passed to the fill scale
\code{limits} argument. Can clip or expand the color range.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}.}
}
\value{
A ggplot2 plot object.
}
\description{
Produces a heatmap of all columns in the design matrix for a \code{baseline_model} object,
with rows corresponding to scans and columns corresponding to regressors. By default,
it draws horizontal lines separating runs (blocks), and rotates the column labels diagonally.
}
\examples{
sframe <- sampling_frame(blocklens = 5, TR = 1)
bmod <- baseline_model(sframe = sframe)
if (requireNamespace("ggplot2", quietly = TRUE)) design_map(bmod)
}
