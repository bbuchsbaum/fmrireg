% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-classes.R
\name{event_basis}
\alias{event_basis}
\title{Create an event set from a ParametricBasis object.}
\usage{
event_basis(
  basis,
  name = NULL,
  onsets,
  blockids = 1,
  durations = 0,
  subset = NULL
)
}
\arguments{
\item{basis}{A \code{ParametricBasis} object (e.g., from \code{BSpline}, \code{PolynomialBasis}).}

\item{name}{Optional name for the event variable. If NULL, uses \code{basis$name}.}

\item{onsets}{Numeric vector of event onsets (seconds).}

\item{blockids}{Numeric vector of block IDs.}

\item{durations}{Numeric vector of event durations (seconds), or a scalar.}

\item{subset}{Optional logical vector indicating which events to keep. If
provided, the vector must match \code{onsets} in length and contain no \code{NA}
values.}
}
\value{
An S3 object of class \code{event} and \code{event_seq}.
}
\description{
This is a user-facing wrapper around the internal \code{event()} constructor,
specifically for creating event sequences modulated by a basis set.
}
\examples{
basis <- BSpline(1:21, 3)
onsets <- seq(0, 20, length.out = 21)
blockids <- rep(1, length(onsets))
ebasis <- event_basis(basis, onsets=onsets, blockids=blockids)
print(ebasis)
levels(ebasis)

}
